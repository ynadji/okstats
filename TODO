* try clustering (after normalization) using the features you have to
identify some things that work in concert? maybe try and build a classifier?
feature selection? use the caret R lib to practice a lil with it
